#include <iostream>
#include<algorithm>
#include <vector>
using namespace std;
int main(void)
{
	
	int count;
	cin >> count;
	vector<int> vec(count);
	for (int i = 0; i < count; i++)
	{
		int num;
		cin >> num;
		vec[i] = num;
	}
	
}
