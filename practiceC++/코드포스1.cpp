#include <iostream>
#include <string>

using namespace std;

int main(void)
{
	int testCase;
	int intword[1000];
	cin >> testCase;
	for (int i = 0; i < testCase; i++)
	{
		string word;
		string word_copy;
		cin >> word;
		int numOFWord = word.length();
		for (int j = 0; j < numOFWord; j++)
		{
			intword[j] = word[j];
		}
		for(int i=0;<)
	}
}